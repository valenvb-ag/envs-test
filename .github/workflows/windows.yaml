on: push

jobs:
  windows:
    runs-on: codebuild-core-GHA-test-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - run: cargo clippy
        shell: bash
      - run: which cargo
        shell: bash
